# 君问公司资产自动登记工具 — 代码结构说明

> 本文档用于说明 `register_asset.py` 的整体代码结构、模块划分与调用关系，便于快速理解程序组织方式。
> 所有行号引用基于清理优化后的 [register_asset.py](file:///d:/project/register_asset/register_asset.py)。

---

## 一、项目概览

程序实现一条「钉钉身份验证 → 本机 MAC 采集 → 生成 Excel → 钉钉发送给管理员」的自动化登记链路。
全部逻辑集中在一个单文件 `register_asset.py`（约 595 行）中，按职责可分为 6 个模块区块。

### 技术栈

| 分类 | 技术 |
|------|------|
| 语言 | Python 3.8+ |
| GUI | Tkinter（ttk + 原生 tk.Label） |
| 网络请求 | requests |
| Excel 处理 | openpyxl |
| 系统信息 | psutil |
| 打包 | PyInstaller |

---

## 二、目录结构

```
register_asset/
├── register_asset.py      # 主程序（全部业务逻辑）
├── config.py              # 敏感配置（不提交，需自行创建）
├── config.example.py      # 配置模板
├── requirements.txt       # 依赖列表
├── register_asset.spec    # PyInstaller 打包配置
├── README.md              # 项目说明
├── 代码结构说明.md         # 本文档
└── .gitignore             # 忽略 config.py / 构建产物
```

---

## 三、模块划分

整个文件自上而下分为 6 个模块区块：

| # | 模块区块 | 行范围 | 职责 |
|---|----------|--------|------|
| 1 | 初始化与配置层 | L12 – L57 | 导入、依赖检测、编码设置、加载敏感配置 |
| 2 | `DingTalkClient` 类 | L60 – L208 | 封装钉钉开放平台全部 API 调用 |
| 3 | 业务工具函数 | L210 – L310 | 生成 Excel / 验证码 / 采集 MAC |
| 4 | `RegDialog` 类 | L312 – L501 | 主交互 GUI（身份验证 + 状态反馈） |
| 5 | 辅助窗口函数 | L503 – L546 | 启动期提示窗口与错误展示 |
| 6 | `main` 主流程 | L549 – L614 | 串联各模块，驱动整体流程 |

---

### 模块 1：初始化与配置层（L12 – L57）

负责程序启动前的环境准备，分三段：

1. **标准库与第三方依赖导入**（L12 – L26）
   - 标准库：`sys / time / random / string / re / os / json / subprocess / tempfile / datetime / tkinter`
   - 第三方：`psutil / requests / openpyxl`，缺失时弹出友好 GUI 提示并退出（L24 – L36）。
2. **Windows 编码设置**（L28 – L39）：将 stdout/stderr 设为 UTF-8，避免 emoji 乱码。
3. **敏感配置加载**（L41 – L57）：从 `config.py` 导入 4 个常量，缺失则弹窗提示并 `exit(1)`。

| 配置常量 | 用途 |
|----------|------|
| `DINGTALK_APP_KEY` | 钉钉应用 AppKey |
| `DINGTALK_APP_SECRET` | 钉钉应用 AppSecret |
| `DINGTALK_AGENT_ID` | 工作通知 AgentId |
| `ADMIN_USER_ID` | 接收登记 Excel 的管理员 userid |

---

### 模块 2：`DingTalkClient` 类（L60 – L208）

封装钉钉开放平台 API。内部维护 `access_token` 并带过期缓存（L65 – L66）。

| 方法 | 行号 | 说明 |
|------|------|------|
| `__init__` | L63 | 保存凭证，初始化 token 缓存 |
| `get_access_token` | L70 | 获取/复用 access_token（提前 60s 过期） |
| `get_department_list` | L89 | 获取部门列表 |
| `get_all_users` | L103 | 遍历全部部门，汇总并去重员工 |
| `get_user_list` | L127 | 分页拉取指定部门员工（仅保留有手机号者） |
| `_send_work_msg` | L152 | 私有：统一发送工作通知，失败抛异常 |
| `send_verify_code` | L168 | 向员工发送 6 位验证码（文本消息） |
| `upload_media` | L176 | 上传文件到钉钉，返回 `media_id` |
| `send_file_to_admin` | L194 | 上传 Excel 并以文件消息发送给管理员 |

---

### 模块 3：业务工具函数（L210 – L310）

三个无状态工具函数，可独立测试：

| 函数 | 行号 | 功能 |
|------|------|------|
| `generate_mac_excel(user_name, macs)` | L210 | 生成 `MAC登记_姓名_时间戳.xlsx` 到临时目录，返回路径 |
| `generate_code()` | L246 | 生成 6 位随机数字验证码 |
| `get_physical_macs()` | L250 | 通过 `psutil` 采集 MAC，调用 `Get-NetAdapter` 按网卡**描述信息**过滤虚拟网卡 |

Excel 表结构：`MAC | 有效日期 | 描述`，描述列填登记人姓名。

---

### 模块 4：`RegDialog` 类（L312 – L501）

继承 `tk.Toplevel`，主交互窗口（400×360，置顶）。负责身份验证与全程非阻塞状态反馈。

**窗口结构（`_setup_ui` L332 – L389）**：
- 标题区：工具名称
- 第一行：姓名下拉（支持输入搜索） + 获取验证码按钮
- 第二行：验证码输入框 + 确定登记按钮
- 底部：原生 `tk.Label` 状态栏（高对比度，自动过期清空）

**方法分组**：

| 分组 | 方法 | 行号 | 说明 |
|------|------|------|------|
| UI 构建 | `_setup_ui` | L332 | 样式与控件初始化 |
| 数据 | `_init_search_data` | L391 | 构建姓名→用户映射并排序 |
| 交互 | `_on_search` | L402 | 输入时实时过滤姓名下拉 |
| 交互 | `_on_select` | L414 | 选中姓名后重置验证码 |
| 状态 | `_set_status` | L422 | 设置 4 级状态提示（info/success/warn/error），定时自动清空 |
| 状态 | `_clear_status` | L454 | 清空状态栏 |
| 业务 | `_send_code` | L460 | 调用 `client.send_verify_code` 发送验证码 |
| 业务 | `_ok` | L478 | 校验验证码，通过后触发回调 |
| 对外 | `show_progress` | L497 | 供主流程调用的进度展示 |

**状态栏定时器管理**：`_status_after_id`（L326）记录上次清空定时器，每次 `_set_status` 先取消未触发的旧定时器，避免进度消息被提前清空。

---

### 模块 5：辅助窗口函数（L503 – L546）

| 函数 | 行号 | 功能 |
|------|------|------|
| `_create_prompt_win(parent, initial_text)` | L503 | 创建屏幕居中的 300×150 提示窗口 |
| `_show_error_in_win(root, win, label, message)` | L524 | 在提示窗口中显示错误并等待用户确认后销毁 root |
| `_show_result_in_win(dialog, message, success)` | L533 | 关闭主对话框，显示登记结果并等待用户确认 |

仅在 `main` 启动初始化阶段（`_show_error_in_win`）和登记结果展示（`_show_result_in_win`）使用。

---

### 模块 6：`main` 主流程（L549 – L614）

整体流程驱动器，含一个内嵌回调 `_process_registration`（L588 – L608）。

**启动阶段**（L551 – L586）：
1. 创建隐藏根窗口，设置统一样式
2. 弹出提示窗显示「正在初始化钉钉客户端…」
3. 实例化 `DingTalkClient`，拉取通讯录
4. 失败则在提示窗内显示错误；成功则关闭提示窗

**交互阶段**（L599 – L601）：创建 `RegDialog`，等待窗口关闭。

**登记回调 `_process_registration`**（验证通过后由 `RegDialog._ok` 经 `after(100, ...)` 触发）：
1. `get_physical_macs()` 采集 MAC
2. `generate_mac_excel()` 生成 Excel
3. `client.send_file_to_admin()` 发送给管理员
4. `_show_result_in_win()` 关闭主对话框，弹出提示窗展示结果并等待用户确认

---

## 四、调用关系图

```
main()
 │
 ├─ _create_prompt_win() ── 启动提示窗
 ├─ DingTalkClient()
 │    └─ get_all_users() ── get_department_list() / get_user_list()
 │         └─ get_access_token()
 ├─ RegDialog(root, users, on_confirmed=_process_registration)
 │    ├─ _setup_ui() / _init_search_data()
 │    ├─ _send_code() ── client.send_verify_code() ── _send_work_msg()
 │    └─ _ok() ── after(_process_registration)
 │
 └─ _process_registration(dialog)
      ├─ get_physical_macs()
      ├─ generate_mac_excel()
      ├─ client.send_file_to_admin()
      │    ├─ upload_media()
      │    └─ _send_work_msg()
      └─ _show_result_in_win(dialog, message, success)
           ├─ dialog.destroy()  关闭主对话框
           ├─ _create_prompt_win(root, message)  弹出辅助窗口
           └─ root.wait_window(win)  等待用户确认
```

---

## 五、关键设计点

1. **敏感配置外置**：`config.py` 不入库（见 [.gitignore](file:///d:/project/register_asset/.gitignore)），通过 `config.example.py` 提供模板。
2. **非阻塞状态反馈**：所有用户提示走 `RegDialog` 底部状态栏（`_set_status`），4 级颜色高对比，定时自动清空，不使用 `messagebox` 弹窗打断流程。
3. **依赖检测前置**：第三方库缺失在导入期即弹出 GUI 提示，而非运行期裸报错。
4. **物理网卡过滤（基于描述信息）**：`get_physical_macs` 通过 Windows `Get-NetAdapter` 获取网卡的 `InterfaceDescription`（由驱动厂商定义，不受用户改名影响），按描述中的关键字（`virtual`/`miniport`/`tunnel`/`kernel debug`/`bluetooth` 等）过滤虚拟网卡。比基于网卡名关键字更稳定可靠，可正确识别 Hyper-V Virtual Ethernet Adapter、Microsoft Wi-Fi Direct Virtual Adapter、WAN Miniport 等虚拟接口。
5. **token 缓存**：`DingTalkClient` 缓存 access_token 并提前 60s 失效，避免重复请求。
6. **回调异步触发**：`_ok` 通过 `after(100, ...)` 调用登记回调，避免阻塞当前事件处理。

---

## 六、依赖说明（requirements.txt）

| 依赖 | 用途 |
|------|------|
| psutil | 采集网卡 MAC 地址 |
| requests | 钉钉 API HTTP 调用 |
| openpyxl | 生成 Excel 登记文件 |
| pyinstaller | 打包为 Windows 可执行文件 |

> 清理说明：已移除未使用的 `pysmb`（早期 NAS 方案遗留，已改为钉钉直接发送文件）。

---

## 七、本次清理优化记录

针对 `register_asset.py` 执行的清理项：

1. **合并重复导入**：移除 config 缺失分支中重复的 `import tkinter / messagebox`（顶部已导入）。
2. **修复失效的依赖检测**：原 `__main__` 中的依赖检测块因顶层导入已先失败而成为死代码，已将其上移至顶部导入区并删除 `__main__` 重复块。
3. **移除未使用参数**：`send_file_to_admin(file_path, name, count, macs)` → `send_file_to_admin(file_path)`，同步更新调用点。
4. **移除未使用依赖**：`requirements.txt` 删除 `pysmb`。

清理后行为与原程序一致，无功能变更。

### 后续修复：虚拟网卡过滤失效

针对实际运行中 `get_physical_macs` 未过滤掉 Hyper-V 等虚拟网卡的问题，进行沙箱诊断并修复：

- **根因**：原关键字 `hyper-v` 不匹配 Windows 上 Hyper-V 虚拟交换机的真实名称前缀 `vEthernet`；`本地连接* 1/2`（Wi-Fi Direct 派生虚拟接口）名称中无任何关键字。
- **修复**：将 `hyper-v` 改为 `vethernet`；新增 `本地连接*` / `local area connection*` / `teredo` / `isatap` / `6to4` / `pseudo-interface` / `tap-windows`。
- **验证**：在沙箱中调用真实 `get_physical_macs()`，返回 3 个真实物理网卡 MAC（外网/内网/WLAN），Hyper-V 虚拟交换机与 Wi-Fi Direct 派生接口均被正确过滤。

### 后续优化：改用网卡描述信息过滤虚拟网卡

基于名称关键字过滤存在局限（用户改网卡名会失效），改用 Windows `Get-NetAdapter` 的 `InterfaceDescription` 字段过滤：

- **方案**：仅基于描述信息过滤（用户确认）。在 `get_physical_macs()` 内通过 PowerShell 调用 `Get-NetAdapter -IncludeHidden` 获取网卡名→描述映射，避免不必要的辅助函数抽象。
- **过滤关键字**：`virtual` / `miniport` / `tunnel` / `pseudo-interface` / `teredo` / `6to4` / `isatap` / `kernel debug` / `bluetooth`。
- **沙箱调研发现**：
  - `Win32_NetworkAdapter.PhysicalAdapter` 字段不可用（看不到 vEthernet）；
  - `Get-NetAdapter.PhysicalAdapter` 返回 None；
  - `InterfaceDescription` 字段最可靠（如 "Hyper-V Virtual Ethernet Adapter"、"Microsoft Wi-Fi Direct Virtual Adapter"）。
- **验证**：在沙箱中调用真实 `get_physical_macs()`，返回 3 个真实物理网卡 MAC，`vEthernet (Default Switch)` 与 `本地连接* 1/2` 均命中 `virtual` 关键字被正确过滤。
